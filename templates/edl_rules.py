# 剪辑规则（每个项目一份，由 Claude 按转写核实后填写；空表 = 只跑自动重录识别）
# 时间都是源片段的秒数。判定用「语音区间中点」是否落在范围内，所以范围可以卡在静音谱边界上。

PAD, MAXGAP, SECTGAP, SECT_MIN = 0.10, 0.22, 0.40, 1.10   # 气口：语速慢用 0.22/0.40，快用 0.16/0.30
SPEED = 1.0   # 整片变速（atempo 保音调）；用户嫌节奏慢 / 要保前 60 秒留存时用 1.1

# 手工丢弃：(clip, 起, 止)  —— 跨片重叠、纯空白、自动前缀法漏掉的换词重录
MANUAL_KILL = [
]

# 强制保留：(clip, 起, 止) —— whisper 把一句幻觉成多遍、自动规则只留到句尾时用
MANUAL_KEEP = [
]
